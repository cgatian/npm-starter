import { Component, OnInit } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'my-app',
    template: `<seed-component></seed-component>`
})
export class AppComponent implements OnInit {
    constructor() { }

    ngOnInit() { }
}